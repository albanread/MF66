[package]
name = "locus-analysis-model"
version = "0.1.0"
edition = "2021"
description = "UI-neutral structured analyzer model types for Locus compiler services."
publish = false

[lib]
name = "locus_analysis_model"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
